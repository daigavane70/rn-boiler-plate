import {View, Text} from 'react-native';
import React from 'react';
import {ProfileScreenProps} from './typings';

export default function Profile(props: ProfileScreenProps) {
  const {} = props;

  return (
    <View>
      <Text>Profile</Text>
    </View>
  );
}
