import {NativeStackScreenProps} from '@react-navigation/native-stack';
import {ScreenProps} from '../../constants/typings';

export type ProfileScreenProps = NativeStackScreenProps<ScreenProps, 'Profile'>;
